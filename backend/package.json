{
  "name": "tiss-flow",
  "version": "1.0.0",
  "private": true,
  "description": "API multi-clínica para operação de faturamento TISS",
  "engines": {
    "node": ">=20 <24"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "name": "tiss-flow-frontend",
  "private": true,
  "description": "Funções puras e testes do frontend do TISS Flow (a UI em si não usa build/bundler).",
  "scripts": {
    "test": "node --test"
  }
}
